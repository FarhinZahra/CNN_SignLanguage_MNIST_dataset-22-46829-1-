# CNN_SignLanguage_MNIST_dataset-22-46829-1-
CNN image classifier built with PyTorch to recognize American Sign Language (ASL) hand gestures using the Sign Language MNIST dataset. Trained on 24 classes (A–Y, excluding J &amp; Z) with BatchNorm, Dropout, and Adam optimizer.
